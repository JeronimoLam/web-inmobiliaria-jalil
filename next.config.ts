import type { NextConfig } from "next";

// `main` serves only the out-of-service page. The full site lives on the
// `pagina-completa` branch.
const nextConfig: NextConfig = {};

export default nextConfig;
